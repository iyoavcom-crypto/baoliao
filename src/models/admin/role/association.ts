// import { Role } from "./index.js";

/**
 * @function associateRole
 * @description 建立 Role 的关联
 */
export function associateRole(): void {
  // 可以在这里添加 Role 的关联，例如 Role.hasMany(User)
  // 但目前 User 模型中已经定义了 belongsTo Role，所以这里是可选的，或者定义反向关系
}
